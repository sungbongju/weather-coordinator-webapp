# 🌤️ WeatherFit — 날씨 기반 코디 추천 웹앱

> **"오늘 뭐 입지?"를 날씨가 대신 알려주는 앱**

---

## 1. 프로젝트 개요

### 1.1 컨셉
- 현재 위치의 실시간 날씨 데이터를 기반으로 최적의 코디를 추천하는 웹앱
- 단순 날씨앱이 아닌, **패션 관점의 날씨 활용** 앱
- 귀여운 일러스트 + 실용적인 코디 추천 조합

### 1.2 타겟 유저
- 매일 아침 "뭐 입지?" 고민하는 사람
- 날씨 확인 → 옷 선택을 한 번에 해결하고 싶은 사람
- 10~30대 남녀 (한국 기준)

### 1.3 핵심 가치
- **원클릭**: 위치 허용만 하면 바로 코디 추천
- **직관적**: 텍스트보다 시각적 일러스트 중심
- **재미**: 날씨별 한 줄 코멘트 + 공유 가능한 카드

---

## 2. 핵심 기능 (MVP)

### 2.1 메인 기능
| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 현재 날씨 표시 | 온도, 체감온도, 강수확률, UV, 미세먼지 | P0 |
| 코디 추천 | 날씨 조건 조합 → 상/하의/아우터/소품 추천 | P0 |
| 코디 일러스트 | 추천 아이템별 일러스트 이미지 표시 | P0 |
| 한 줄 코멘트 | "오늘은 가디건 하나 챙기면 딱!" 같은 AI 멘트 | P1 |
| 오전/오후 분리 추천 | 일교차 큰 날 오전·오후 다른 코디 | P1 |
| SNS 공유 카드 | 오늘의 코디를 예쁜 카드 이미지로 생성 | P2 |

### 2.2 부가 기능 (v2 이후)
| 기능 | 설명 | 우선순위 |
|------|------|----------|
| 내 옷장 등록 | 사진 업로드 → 배경 제거 → 카테고리 분류 | v2 |
| AI 개인화 추천 | 등록된 옷 기반 맞춤 코디 생성 | v2 |
| 주간 코디 플래너 | 7일 예보 기반 일주일 코디 미리 제안 | v2 |
| 쇼핑몰 연동 | 추천 아이템 → 구매 링크 (무신사, 쿠팡 등) | v3 |

---

## 3. 기술 스택

### 3.1 프론트엔드
```
Framework: Next.js 14+ (App Router)
Language:  TypeScript
Styling:   Tailwind CSS
State:     Zustand (경량 상태 관리)
Deploy:    Vercel (무료 티어)
```

### 3.2 백엔드 / API
```
서버:      Next.js API Routes (서버리스)
날씨 API:  Google Weather API (현재 무료 Preview)
위치:      Geolocation API (브라우저 내장)
미세먼지:  Google Air Quality API (선택)
```

### 3.3 이미지 / 에셋
```
일러스트:  AI 생성 (DALL-E / Midjourney) + Flaticon 보충
포맷:      SVG (아이콘) + WebP (일러스트)
저장:      /public/assets/clothing/ 내 정적 파일
```

### 3.4 선택적 추가 도구
```
AI 코멘트: Claude API (한 줄 코멘트 생성, 선택)
분석:      Vercel Analytics (무료)
도메인:    Cloudflare Registrar (최저가)
```

---

## 4. 날씨-코디 매핑 로직

### 4.1 온도 기반 카테고리 (핵심)

```
FREEZING:  ~ -5°C    → 패딩/롱패딩, 히트텍, 기모바지, 목도리, 장갑, 방한모
COLD:      -5 ~ 5°C  → 코트/숏패딩, 니트, 기모바지, 머플러
CHILLY:    5 ~ 12°C  → 자켓/트렌치코트, 맨투맨, 긴바지
MILD:      12 ~ 17°C → 가디건/얇은자켓, 긴팔셔츠, 면바지
WARM:      17 ~ 23°C → 얇은 긴팔/셔츠, 면바지/치노
HOT:       23 ~ 28°C → 반팔, 반바지/린넨팬츠
SCORCHING: 28°C ~    → 민소매/크롭, 숏팬츠, 샌들
```

### 4.2 추가 조건 레이어

```
비/눈 (강수확률 > 50%):
  → +우산, 방수자켓/레인부츠

바람 강함 (풍속 > 30km/h):
  → +바람막이, 안날리는 옷 추천, 모자 비추

UV 높음 (UV지수 ≥ 6):
  → +선글라스, 모자, 선크림 안내

미세먼지 나쁨 (AQI > 100):
  → +마스크 안내

일교차 큼 (최고-최저 ≥ 10°C):
  → 레이어링 추천 (걸칠 수 있는 아우터 강조)
```

### 4.3 매핑 데이터 구조 예시

```typescript
// types/outfit.ts

interface ClothingItem {
  id: string;
  name: string;           // "패딩"
  nameEn: string;         // "Puffer Jacket"
  category: Category;     // "OUTER" | "TOP" | "BOTTOM" | "ACCESSORY" | "SHOES"
  imageUrl: string;       // "/assets/clothing/outer/puffer.webp"
  tempRange: [number, number]; // [-20, 5]
  tags: string[];         // ["방한", "캐주얼"]
}

interface OutfitRecommendation {
  outer?: ClothingItem;
  top: ClothingItem;
  bottom: ClothingItem;
  shoes: ClothingItem;
  accessories: ClothingItem[];
  comment: string;        // "오늘 패딩 없으면 후회합니다 🥶"
}

type Category = "OUTER" | "TOP" | "BOTTOM" | "SHOES" | "ACCESSORY";

type TempLevel = 
  | "FREEZING" 
  | "COLD" 
  | "CHILLY" 
  | "MILD" 
  | "WARM" 
  | "HOT" 
  | "SCORCHING";
```

---

## 5. 옷 일러스트 에셋 목록

### 5.1 필요 이미지 총 목록 (MVP 기준 ~50개)

**아우터 (OUTER) — 8종**
- [ ] 롱패딩
- [ ] 숏패딩
- [ ] 코트
- [ ] 트렌치코트
- [ ] 자켓 (데님/봄)
- [ ] 가디건
- [ ] 바람막이
- [ ] 방수자켓 (레인코트)

**상의 (TOP) — 10종**
- [ ] 히트텍 (이너)
- [ ] 니트/스웨터
- [ ] 맨투맨/후드
- [ ] 긴팔 셔츠
- [ ] 긴팔 티셔츠
- [ ] 반팔 티셔츠
- [ ] 린넨 셔츠
- [ ] 민소매/탱크탑
- [ ] 블라우스
- [ ] 크롭탑

**하의 (BOTTOM) — 7종**
- [ ] 기모바지
- [ ] 청바지
- [ ] 면바지/치노
- [ ] 슬랙스
- [ ] 린넨팬츠
- [ ] 반바지
- [ ] 숏팬츠/치마

**신발 (SHOES) — 6종**
- [ ] 부츠
- [ ] 레인부츠
- [ ] 운동화
- [ ] 로퍼/구두
- [ ] 캔버스/슬립온
- [ ] 샌들/슬리퍼

**소품 (ACCESSORY) — 10종**
- [ ] 목도리/머플러
- [ ] 장갑
- [ ] 방한모/비니
- [ ] 모자 (캡/버킷햇)
- [ ] 선글라스
- [ ] 우산
- [ ] 마스크
- [ ] 선크림 (아이콘)
- [ ] 가방 (기본)
- [ ] 워머/핫팩 (아이콘)

### 5.2 일러스트 스타일 가이드
```
- 스타일: 플랫 일러스트 (미니멀, 깔끔한 선)
- 색상: 파스텔 톤 기본 + 계절별 컬러 변주
- 크기: 512x512px (원본), 사용 시 리사이징
- 포맷: WebP (용량 최적화) + SVG (아이콘)
- 배경: 투명
- 일관성: 동일한 프롬프트/스타일로 AI 일괄 생성 권장
```

### 5.3 AI 이미지 생성 프롬프트 예시
```
"Flat illustration of a [clothing item], minimal style,
clean lines, pastel colors, transparent background,
no shadow, centered, fashion illustration style, 512x512"
```

---

## 6. 프로젝트 구조

```
weather-outfit-app/
├── public/
│   ├── assets/
│   │   ├── clothing/
│   │   │   ├── outer/        # 아우터 일러스트
│   │   │   ├── top/          # 상의 일러스트
│   │   │   ├── bottom/       # 하의 일러스트
│   │   │   ├── shoes/        # 신발 일러스트
│   │   │   └── accessory/    # 소품 일러스트
│   │   ├── weather/          # 날씨 아이콘/배경
│   │   └── ui/               # UI 에셋
│   └── favicon.ico
├── src/
│   ├── app/
│   │   ├── layout.tsx
│   │   ├── page.tsx          # 메인 페이지
│   │   ├── globals.css
│   │   └── api/
│   │       └── weather/
│   │           └── route.ts  # Google Weather API 프록시
│   ├── components/
│   │   ├── WeatherCard.tsx       # 현재 날씨 표시
│   │   ├── OutfitRecommend.tsx   # 코디 추천 메인
│   │   ├── ClothingItem.tsx      # 개별 아이템 카드
│   │   ├── OutfitComment.tsx     # 한 줄 코멘트
│   │   ├── ShareCard.tsx         # SNS 공유용 카드
│   │   ├── LocationPicker.tsx    # 위치 선택/검색
│   │   └── TemperatureToggle.tsx # 체감온도/실제온도 토글
│   ├── lib/
│   │   ├── weather.ts        # Google Weather API 호출
│   │   ├── airQuality.ts     # Air Quality API 호출 (선택)
│   │   ├── outfitEngine.ts   # 날씨→코디 매핑 핵심 로직
│   │   ├── clothingData.ts   # 의류 데이터 (정적)
│   │   └── comments.ts       # 코멘트 데이터/생성
│   ├── types/
│   │   ├── weather.ts        # 날씨 관련 타입
│   │   └── outfit.ts         # 코디 관련 타입
│   ├── hooks/
│   │   ├── useWeather.ts     # 날씨 데이터 커스텀 훅
│   │   ├── useGeolocation.ts # 위치 정보 훅
│   │   └── useOutfit.ts      # 코디 추천 훅
│   └── store/
│       └── weatherStore.ts   # Zustand 스토어
├── .env.local                # API 키 (로컬)
├── .env.example              # API 키 템플릿
├── next.config.js
├── tailwind.config.ts
├── tsconfig.json
├── package.json
├── PROJECT_PLAN.md           # 이 문서
├── CLAUDE.md                 # Claude Code 지침서
└── README.md
```

---

## 7. API 설정 가이드

### 7.1 Google Weather API 설정

```bash
# 1. Google Cloud Console에서 프로젝트 생성
# https://console.cloud.google.com/

# 2. Weather API 활성화
# APIs & Services → "Weather API" 검색 → Enable

# 3. API 키 생성
# APIs & Services → Credentials → Create Credentials → API Key

# 4. .env.local에 추가
GOOGLE_WEATHER_API_KEY=your_api_key_here
```

### 7.2 API 호출 예시

```typescript
// lib/weather.ts

const WEATHER_BASE_URL = "https://weather.googleapis.com/v1";

export async function getCurrentWeather(lat: number, lng: number) {
  const res = await fetch(
    `${WEATHER_BASE_URL}/currentConditions:lookup?` +
    `key=${process.env.GOOGLE_WEATHER_API_KEY}` +
    `&location.latitude=${lat}` +
    `&location.longitude=${lng}`,
    { next: { revalidate: 600 } } // 10분 캐시
  );
  return res.json();
}

export async function getForecast(lat: number, lng: number, days = 3) {
  const res = await fetch(
    `${WEATHER_BASE_URL}/forecast/days:lookup?` +
    `key=${process.env.GOOGLE_WEATHER_API_KEY}` +
    `&location.latitude=${lat}` +
    `&location.longitude=${lng}` +
    `&days=${days}`,
    { next: { revalidate: 1800 } } // 30분 캐시
  );
  return res.json();
}
```

### 7.3 비용
- **Weather API**: Preview 기간 무료 (2025년 기준)
- **Air Quality API**: 월 500건 무료
- **Geolocation**: 브라우저 내장 (무료)

---

## 8. 핵심 로직 설계

### 8.1 outfitEngine.ts (코디 추천 핵심)

```typescript
// lib/outfitEngine.ts — 핵심 추천 알고리즘 개요

export function recommendOutfit(weather: WeatherData): OutfitRecommendation {
  // Step 1: 체감온도 기반 온도 레벨 결정
  const tempLevel = getTempLevel(weather.feelsLikeTemperature);
  
  // Step 2: 기본 코디 세트 선택 (온도 기준)
  const baseOutfit = getBaseOutfit(tempLevel);
  
  // Step 3: 추가 조건 레이어 적용
  if (weather.precipitation.probability > 50) {
    addRainLayer(baseOutfit);     // 우산, 방수아이템 추가
  }
  if (weather.uvIndex >= 6) {
    addUVProtection(baseOutfit);  // 선글라스, 모자 추가
  }
  if (weather.wind.speed > 30) {
    addWindProtection(baseOutfit); // 바람막이 교체
  }
  if (isDailyTempGapLarge(weather)) {
    addLayering(baseOutfit);      // 레이어링 강조
  }
  
  // Step 4: 코멘트 생성
  baseOutfit.comment = generateComment(tempLevel, weather);
  
  return baseOutfit;
}
```

### 8.2 코멘트 예시 데이터

```typescript
// lib/comments.ts

export const COMMENTS: Record<TempLevel, string[]> = {
  FREEZING: [
    "밖에 나가면 코가 빨개져요. 완전무장 필수! 🥶",
    "이 추위엔 패딩이 예의입니다",
    "핫팩 2개는 기본이에요",
  ],
  COLD: [
    "코트 없이 나가면 후회합니다",
    "니트 위에 코트, 완벽한 조합!",
    "목도리 하나면 체감 5도 올라가요",
  ],
  CHILLY: [
    "자켓 하나 걸치면 딱 좋아요",
    "아침엔 쌀쌀하니 겉옷 챙기세요!",
    "가을 감성 뽑기 좋은 날씨 🍂",
  ],
  MILD: [
    "가디건 하나면 충분해요",
    "걸칠 거 하나만 챙기세요~",
    "제일 입기 좋은 날씨!",
  ],
  WARM: [
    "긴팔 하나면 딱이에요",
    "얇은 셔츠가 정답인 날씨",
    "바람 불면 시원~ 완벽한 외출 날씨 ☀️",
  ],
  HOT: [
    "반팔 반바지 국룰이죠",
    "시원하게 입으세요! 더워요 🔥",
    "선크림 필수! UV 주의보",
  ],
  SCORCHING: [
    "밖에 나가지 마세요... 진심 🫠",
    "에어컨이 제일 좋은 옷입니다",
    "물 많이 드세요! 최소한의 옷차림으로!",
  ],
};
```

---

## 9. UI/UX 디자인 방향

### 9.1 화면 구성 (모바일 퍼스트)

```
┌─────────────────────────┐
│  📍 전주시               │ ← 현재 위치
│  ☁️ 15°C (체감 12°C)    │ ← 현재 날씨
│  강수 20% · 바람 8km/h  │
├─────────────────────────┤
│                         │
│   [코디 일러스트 영역]    │ ← 추천 코디 시각화
│   🧥  👕  👖  👟       │
│                         │
│  "자켓 하나 걸치면        │
│   딱 좋은 날씨에요!"     │ ← 한 줄 코멘트
│                         │
├─────────────────────────┤
│  상의: 긴팔 셔츠         │
│  하의: 면바지             │ ← 아이템 상세
│  아우터: 가벼운 자켓      │
│  소품: 없음              │
├─────────────────────────┤
│  [오전 코디] [오후 코디]  │ ← 시간대별 탭
├─────────────────────────┤
│  [ 📤 오늘의 코디 공유 ] │ ← 공유 버튼
└─────────────────────────┘
```

### 9.2 디자인 키워드
- **톤**: 밝고 부드러운 파스텔 (계절마다 색상 변주)
- **폰트**: Pretendard (한글) + Inter (영문)
- **무드**: 깔끔한 미니멀 + 약간의 귀여움
- **애니메이션**: 코디 전환 시 부드러운 트랜지션

### 9.3 반응형 브레이크포인트
```
Mobile:  ~640px   (기본, 핵심 UX)
Tablet:  641~1024px
Desktop: 1025px~  (좌: 날씨 / 우: 코디 레이아웃)
```

---

## 10. 개발 로드맵

### Phase 1: 기반 세팅 (Day 1)
- [ ] Next.js + TypeScript + Tailwind 프로젝트 생성
- [ ] 프로젝트 구조 세팅 (폴더, 타입 정의)
- [ ] Google Cloud 프로젝트 생성 + Weather API 활성화
- [ ] .env.local 설정 + API 키 발급
- [ ] CLAUDE.md 작성 (Claude Code 지침)

### Phase 2: 날씨 데이터 연동 (Day 2)
- [ ] Geolocation 훅 구현 (useGeolocation)
- [ ] Weather API 서버 라우트 구현 (/api/weather)
- [ ] 날씨 데이터 타입 정의
- [ ] WeatherCard 컴포넌트 제작
- [ ] 기본 날씨 표시 화면 완성

### Phase 3: 코디 추천 엔진 (Day 3)
- [ ] 의류 데이터 구조화 (clothingData.ts)
- [ ] 온도 레벨 분류 로직 구현
- [ ] 추가 조건 레이어 로직 (비, UV, 바람 등)
- [ ] outfitEngine 핵심 함수 완성
- [ ] 코멘트 데이터 및 랜덤 선택 로직

### Phase 4: UI 구현 (Day 4-5)
- [ ] 일러스트 에셋 준비 (AI 생성 또는 수집)
- [ ] OutfitRecommend 메인 컴포넌트
- [ ] ClothingItem 카드 컴포넌트
- [ ] 오전/오후 탭 구현
- [ ] 모바일 반응형 레이아웃
- [ ] 애니메이션/트랜지션

### Phase 5: 마무리 + 배포 (Day 6)
- [ ] ShareCard 컴포넌트 (html2canvas)
- [ ] 메타 태그 + OG 이미지
- [ ] Vercel 배포
- [ ] 도메인 연결 (선택)
- [ ] README.md 작성

### Phase 6: v2 확장 (이후)
- [ ] 사용자 옷장 등록 기능
- [ ] AI 개인화 추천
- [ ] 주간 코디 플래너
- [ ] PWA 지원 (홈 화면 추가)

---

## 11. CLAUDE.md 가이드 (Claude Code용)

```markdown
# CLAUDE.md — WeatherFit 프로젝트 지침

## 프로젝트 소개
날씨 기반 코디 추천 웹앱. Next.js 14+ App Router + TypeScript.

## 기술 스택
- Next.js 14+ (App Router)
- TypeScript (strict mode)
- Tailwind CSS
- Zustand (상태 관리)
- Google Weather API

## 코딩 컨벤션
- 컴포넌트: PascalCase (WeatherCard.tsx)
- 유틸/훅: camelCase (useWeather.ts)
- 타입: types/ 폴더에 분리
- API 키: 절대 하드코딩 금지, .env.local 사용
- 한국어 주석 사용 가능

## 주요 파일 역할
- src/lib/outfitEngine.ts → 코디 추천 핵심 로직
- src/lib/clothingData.ts → 의류 정적 데이터
- src/lib/weather.ts → Google Weather API 호출
- src/app/api/weather/route.ts → API 프록시 (키 보호)

## API 사용 시 주의
- Google Weather API 키는 서버 사이드에서만 사용
- 클라이언트에서 직접 호출 금지 (프록시 라우트 경유)
- 캐싱: 현재 날씨 10분, 예보 30분

## 테스트
- 위치 권한 거부 시 기본값: 서울 (37.5665, 126.9780)
- 오프라인/API 실패 시 graceful fallback 처리
```

---

## 12. 경쟁 앱 분석 & 차별화

### 12.1 기존 앱들
| 앱 | 특징 | 약점 |
|---|------|------|
| Weather Fit (iOS) | 아바타 기반, 프리미엄 모델 | iOS 전용, 유료 |
| Oshare Weather | 일본풍 일러스트, 550만 DL | 일본 중심, 한국 스타일 아님 |
| Fits | 옷장 등록+AI 추천 | 진입장벽 높음 (등록 필요) |
| Cladwell | ChatGPT 연동 | AI 추천 정확도 낮음 |

### 12.2 우리의 차별화
- **한국 날씨·패션 최적화**: 한국 사계절 + 미세먼지 고려
- **웹앱**: 설치 없이 브라우저에서 바로 사용
- **제로 진입장벽**: 옷장 등록 없이 바로 추천
- **한 줄 코멘트**: 딱딱한 추천이 아닌 재치있는 한마디
- **공유 기능**: SNS 공유 카드로 바이럴 가능성

---

## 13. 도메인 후보

```
weatherfit.kr          → 한국 타겟 명확
mwo-ipji.com          → "뭐입지" 발음
onul-codi.com          → "오늘코디"
today-outfit.kr        → 직관적
ot-chwucheon.com       → "옷추천"
nalssicodi.com         → "날씨코디"
```

---

## 14. 참고 자료

- [Google Weather API 공식 문서](https://developers.google.com/maps/documentation/weather/overview)
- [Google Air Quality API](https://developers.google.com/maps/documentation/air-quality)
- [Google Pollen API](https://developers.google.com/maps/documentation/pollen/overview)
- [Next.js 공식 문서](https://nextjs.org/docs)
- [Tailwind CSS](https://tailwindcss.com/docs)
- [Zustand](https://zustand-demo.pmnd.rs/)
- [Flaticon 의류 아이콘](https://www.flaticon.com/search?word=clothing)
- [Freepik 패션 일러스트](https://www.freepik.com/search?query=fashion+illustration+flat)

---

> **마지막 업데이트**: 2026-02-16
> **작성**: Claude + 봉주
> **상태**: MVP 기획 완료 → 개발 준비 단계
